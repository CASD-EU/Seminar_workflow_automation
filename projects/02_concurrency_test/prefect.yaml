# prefect.yaml
name: pengfei_prefect_concurrency
prefect-version: 3.4.24

# it can have multiple deployments
deployments:
  # Deployment 1: simple ETL flow
  - name: pengfei_long_running_job
    entrypoint: flows/long_running_job.py:start_long_running_flow
    description: "This simple workflow takes time to finish."
    work_pool:
      name: pliu-pool
      type: process
      tags: [test, local]
