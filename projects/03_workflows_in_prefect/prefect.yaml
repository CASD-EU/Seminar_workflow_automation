# prefect.yaml
name: workflow_in_prefect
prefect-version: 3.4.24

# it can have multiple deployments
deployments:
  # Deployment 2: ETL flow
  - name: task_flow_param_example
    version: "1.0.0"
    tags: [test, local]
    description: "This workflow test task and flow with various parameters"
    entrypoint: task_flow_param_example.py:my_flow
    work_pool:
      name: pliu-pool
      work_queue_name: default
      type: process

